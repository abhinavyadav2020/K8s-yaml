kind: Pod
apiVersion: v1
metadata:
  name: pod-annotations
  annotations: 
    description: Created this yml file to learn annotations in metadata
spec:
  containers:
    - name: c01
      image: ubuntu
      command: ["/bin/bash", "-c", "while true; do echo Hello-Bhupinder; sleep 5 ; done"]
  restartPolicy: Never
